# Cleanup policy

Composition Studio is kept intentionally lean. One-off migration helpers, temporary patch files and duplicate version snapshots should not accumulate on `main` once their changes are consolidated.
